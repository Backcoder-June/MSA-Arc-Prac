server:
  port: 8900
spring:
  application:
    name: authentication-server
  profiles:
    active: test
  cloud:
    config:
      uri: http://localhost:8080
    loadbalancer:
      ribbon:
        enabled: false
