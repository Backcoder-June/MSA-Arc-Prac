spring:
  application:
    name: eureka-server
  profiles:
    active: docker
  cloud:
    config:
      uri: http://config-server:8080
    loadbalancer:
      ribbon:
        enabled: false
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
server:
  port: 8761